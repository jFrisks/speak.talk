A function that is called main. It returns an integer by running the following code:
  - if 3 is greater than 2, do the following:
    - finally return 2;
  .
  - if 2 is less than 1, do the following:
    - finally return 3;
  .
  the function that is called a;
  - finally return 1;
.

A function that is called a. It returns an integer by running the following code:
  - if 3 is greater than 2, do the following:
    - finally return 2;
  .
  - if 2 is less than 1, do the following:
    - finally return 3;
  .
  - finally return 1;
.