A function that is called main. It returns an integer by running the following code:
  - run the function that is called print and uses the arguments of 7 subtracted by 2 multiplied with the expression, 5 divided by2.
  - finally return 1.
.