A function that is called main. It returns an integer by running the following code:
  f(3);
  - finally return 1;
}

A function that is called f and uses the arguments of an integer that is called a. It returns an integer by running the following code:
  print(a);
  - finally return 1;
}