A function that is called main. It returns an integer by running the following code:
  - run the function that is called f and uses the arguments of 1,2,3;
  - finally return 3;
.

A function that is called f and uses the arguments of an integer that is called a, an integer that is called b, an integer that is called c. It returns an integer by running the following code:
  - finally return c;
.