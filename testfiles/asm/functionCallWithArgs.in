A function that is called main and returns an integer. It runs the following:
  - run the function that is called f and uses the arguments of 1,2,3.
  - finally return 3.
.

A function that is called f and returns an integer by using the arguments of an integer that is called a, an integer that is called b, an integer that is called c. It runs the following:
  - finally return c.
.