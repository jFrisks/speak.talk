A function that is called main. It returns an integer by running the following code:
  - loop while 3 is less than 2. Run the following code each time:
    - run the function that is called print and uses the arguments of 10;
  .
  - run the function that is called print and uses the arguments of 20;
  - finally return 1;
.