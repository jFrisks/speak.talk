A function that is called main and returns an integer. It runs the following:
  - run the function that is called print and uses the arguments of 1.
  the function that is called f.
  the function that is called g.
  - finally return 1.
.

A function that is called f and returns an integer. It runs the following:
  - run the function that is called print and uses the arguments of 2.
  - finally return 2.
.

A function that is called g and returns an integer. It runs the following:
  - run the function that is called print and uses the arguments of 3.
  - finally return 3.
.