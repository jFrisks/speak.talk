A function that is called main. It returns an integer by running the following code:
  - an integer that is called x.
  - an integer that is called y and has the value of 10;
  x = y + 2;
  print(42);
  - finally return f(y + x);
}

int f(an integer that is called x){
  print(x);
  - an integer that is called y and has the value of 2;
  print(y);
  - finally return 1;
}