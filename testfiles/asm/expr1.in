A function that is called main and returns an integer. It runs the following:
  - run the function that is called print and uses the arguments of 3 added to the expression, 4 divided by2,multiplied with3.
  - finally return 1.
.