A function that is called a and returns an integer. It runs the following:
    - finally return 1.
.

A function that is called b and returns an integer. It runs the following:
    - an integer that is called x and has the value of a.
    - finally return x.
.