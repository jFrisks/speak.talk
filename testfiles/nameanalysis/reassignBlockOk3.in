A function that is called main and returns an integer. It runs the following:
    - an integer that is called x and has the value of 5.
    - if 3 is greater than 2, do the following:
        - an integer that is called x and has the value of 4.
    .
.