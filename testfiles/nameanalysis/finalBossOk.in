A function that is called main. It returns an integer by running the following code:
      // depth = 1
      - an integer that is called i and has the value of 100;
      while (i > 0) {
        // depth = 2
        if (i == 5) {
// depth = 3
print(i); }
i = i - 1; }
      if (i == 0) {
        // depth = 2
        print(i);
      }
      // depth = 1
      - finally return 0;
}