A function that is called f and returns an integer by using the arguments of an integer that is called x, an integer that is called y, an integer that is called z. It runs the following: .
A function that is called g and returns an integer by using the arguments of an integer that is called x, an integer that is called y, an integer that is called h. It runs the following: .