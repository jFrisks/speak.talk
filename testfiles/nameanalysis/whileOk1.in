A function that is called main and returns an integer. It runs the following:
    - an integer that is called a and has the value of 3.
    - loop while 3 is greater than 2. Run the following  each time:
        - an integer that is called a and has the value of 4.
        - finally return a.
    .
.