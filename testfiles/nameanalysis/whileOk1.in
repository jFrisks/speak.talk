A function that is called main. It returns an integer by running the following code:
    - an integer that is called a and has the value of 3;
    - loop while 3 is greater than 2. Run the following code each time:
        - an integer that is called a and has the value of 4;
        - finally return a;
    .
.