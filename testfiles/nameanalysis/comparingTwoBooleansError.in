A function that is called f and returns an integer by using the arguments of an integer that is called a, an integer that is called b. It runs the following:
  - if  the expression, the variable that is called a is less than b, is equal to  the expression, the variable that is called a is equal to b, do the following:
    - finally return a.
  .
  - finally return b.
.