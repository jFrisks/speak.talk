A function that is called f and uses the arguments of an integer that is called a, an integer that is called b. It returns an integer by running the following code:
  if ((a < b) == (a == b)) {
    - finally return a;
  }
  - finally return b;
}