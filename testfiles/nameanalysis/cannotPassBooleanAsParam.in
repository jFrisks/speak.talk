A function that is called f and returns an integer by using the arguments of an integer that is called x. It runs the following:
  - finally return the value of the function that is called f and uses the arguments of the variable that is called x is equal to 0.
.