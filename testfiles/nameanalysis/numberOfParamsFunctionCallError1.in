A function that is called a and returns an integer by using the arguments of an integer that is called x, an integer that is called y. It runs the following:
    - finally return the variable that is called x multiplied with the variable that is called y.
.

A function that is called b and returns an integer. It runs the following:
    - run the function that is called a and uses the arguments of 3.
.