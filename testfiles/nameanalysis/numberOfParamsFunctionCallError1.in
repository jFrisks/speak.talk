A function that is called a and uses the arguments of an integer that is called x, an integer that is called y. It returns an integer by running the following code:
    - finally return the variable that is called x multiplied with y.
.

A function that is called b. It returns an integer by running the following code:
    - run the function that is called a and uses the arguments of 3.
.