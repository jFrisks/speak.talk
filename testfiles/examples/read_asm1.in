A function that is called main and returns an integer. It runs the following:
  - an integer that is called a and has the value of the function that is called read.
  - the function that is called print and uses the arguments of a.
  - finally return 1.
.