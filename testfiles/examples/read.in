A function that is called main. It returns an integer by running the following code:
    - an integer that is called x has the value of the function that is called read.
    - the function that is called print and uses the arguments of x.
    - finally return x.
.