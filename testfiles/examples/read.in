A function that is called main and returns an integer. It runs the following:
    - an integer that is called x and has the value of the function that is called read.
    - the function that is called print and uses the arguments of x.
    - finally return x.
.