A function that is called main and returns an integer. It runs the following:
  - an integer that is called i and has the value of 100.
  - if 3 is greater than 1, do the following:
    - finally return 1.
  .
  - finally return 0.
.