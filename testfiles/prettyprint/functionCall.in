A function that is called b and returns an integer. It runs the following:
    - an integer that is called x.
    - the variable that is called x has the value of 10.
.
A function that is called a and returns an integer. It runs the following:
    - an integer that is called x.
    - an integer that is called y.
    - the variable that is called x has the value of 10.
    - the variable that is called y has the value of 11.
    - run the function that is called print and uses the arguments of the value of the function that is called b that uses the arguments of the variable that is called x, the variable that is called y.
.