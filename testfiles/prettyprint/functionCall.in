A function that is called b. It returns an integer by running the following code:
    - an integer that is called x.
    - the variable that is called x = 10;
.
A function that is called a. It returns an integer by running the following code:
    - an integer that is called x.
    - an integer that is called y.
    - the variable that is called x = 10;
    - the variable that is called y = 11;
    - run the function that is called print and uses the arguments of the value of the function b that uses the arguments of the variable that is called x, the variable that is called y;
.