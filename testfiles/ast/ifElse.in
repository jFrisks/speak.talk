A function that is called a and returns an integer. It runs the following:
 - an integer that is called x.
 - the variable that is called x has the value of 10.
 - if the variable that is called x is greater than 3, do the following:
  - an integer that is called z.
  - the variable that is called z has the value of 3.
 .
 - else, do:
  - run the function that is called print and uses the arguments of 3.
 .
.