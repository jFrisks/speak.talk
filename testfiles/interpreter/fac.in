A function that is called fac and returns an integer by using the arguments of an integer that is called n. It runs the following:
    - if the variable that is called n is less or equal to 1, do the following: - return 1. .
 - else, do: - return the value of the function that is called fac that uses the arguments of the variable that is called n subtracted by1 multiplied with n. .
.

A function that is called f and returns an integer. It runs the following:
    - run the function that is called print and uses the arguments of the value of the function that is called fac that uses the arguments of 5.
.

A function that is called main and returns an integer. It runs the following:
    the function that is called f.
.