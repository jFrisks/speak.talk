A function that is called main. It returns an integer by running the following code:
  - an integer that is called x and has the value of 0;
  - an integer that is called sum and has the value of 0;
  while (x < 10) {
    if (x < 3) {
      - an integer that is called x.
      x = 13;
      sum = sum + x;
    }
    sum = sum + 1;
    x = x + 1;
  }
  print(sum);
  - finally return 0;
}