A function that is called main. It returns an integer by running the following code:
    - an integer that is called i has the value of 0. // Declaration of i
    - if the variable that is called i is equal to 0, do the following:
        - an integer that is called i has the value of 1. // Another declaration of i
        - if the variable that is called i is equal to 1, do the following:
            - an integer that is called i has the value of 2. // A third declaration of i
            - run the function that is called print and uses the arguments of the variable that is called i. // Prints 2
        .
        - run the function that is called print and uses the arguments of the variable that is called i. // Prints 1
    .
    - run the function that is called print and uses the arguments of the variable that is called i. // Prints 0
.