A function that is called main and returns an integer. It runs the following:
    - run the function that is called print and uses the arguments of the negative of 2.
    - run the function that is called print and uses the arguments of the negative of the negative of 2.
.