A function that is called main. It returns an integer by running the following code:
    - run the function that is called print and uses the arguments of 3 multiplied with2.
    - run the function that is called print and uses the arguments of 3 added to2.
.