A function that is called main and returns an integer. It runs the following:
    - run the function that is called print and uses the arguments of 1 added to2 multiplied with3.
    - run the function that is called print and uses the arguments of 4 multiplied with5.
.