A function that is called main. It returns an integer by running the following code:
    - run the function that is called print and uses the arguments of 6 divided by 2.
.


A function that is called main. It returns an integer by running the following code:
    - an integer that is called x has the value of 10.
    - finally return the function that is called the variable that is called a added to the value of the function b that uses the arguments of the variable that is called x added to the function that is called c.
.