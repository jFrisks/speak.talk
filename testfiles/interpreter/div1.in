A function that is called main and returns an integer. It runs the following:
    - run the function that is called print and uses the arguments of 6 divided by 2.
.


A function that is called main and returns an integer. It runs the following:
    - an integer that is called x and has the value of 10.
    - finally return the function that is called the variable that is called a added to the value of the function that is called b that uses the arguments of the variable that is called x added to the function that is called c.
.