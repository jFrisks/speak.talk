A function that is called add and returns an integer by using the arguments of an integer that is called a. It runs the following:
    - run the function that is called print and uses the arguments of the variable that is called a.
.

A function that is called main and returns an integer. It runs the following:
    - an integer that is called i and has the value of 10.
    - run the function that is called add and uses the arguments of the variable that is called i. // Call with 2 arguments: first value of i, second value 20.
    the function that is called sub.
.

A function that is called sub and returns an integer. It runs the following:
    - run the function that is called print and uses the arguments of 11.
.