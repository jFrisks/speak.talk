A function that is called main. It returns an integer by running the following code:
    - an integer that is called x has the value of 3.
    - run the function that is called print and uses the arguments of the function that is called a.
.

A function that is called a. It returns an integer by running the following code:
    - an integer that is called x has the value of 2.
.