A function that is called main and returns an integer. It runs the following:
    - an integer that is called x and has the value of 3.
    - run the function that is called print and uses the arguments of the function that is called a.
.

A function that is called a and returns an integer. It runs the following:
    - an integer that is called x and has the value of 2.
.