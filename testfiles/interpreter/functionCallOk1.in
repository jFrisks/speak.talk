A function that is called add and returns an integer by using the arguments of an integer that is called a, an integer that is called b. It runs the following:
    - run the function that is called print and uses the arguments of the variable that is called a added to b.
.

A function that is called main and returns an integer. It runs the following:
    - an integer that is called i and has the value of 10.
    - run the function that is called add and uses the arguments of the variable that is called i, 20. // Call with 2 arguments: first value of i, second value 20.
.