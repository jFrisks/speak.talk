A function that is called myprint and returns an integer by using the arguments of an integer that is called x. It runs the following:
  - run the function that is called print and uses the arguments of the variable that is called x.
.
A function that is called main and returns an integer. It runs the following:
  - run the function that is called myprint and uses the arguments of 144.
.