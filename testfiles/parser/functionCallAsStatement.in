A function that is called myprint and uses the arguments of an integer that is called x. It returns an integer by running the following code:
  print(x);
}
A function that is called main. It returns an integer by running the following code:
  myprint(144);
}