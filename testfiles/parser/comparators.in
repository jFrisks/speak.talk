A function that is called a. It returns an integer by running the following code:
     - an integer that is called b.
     - an integer that is called c.
     - an integer that is called d.
     - an integer that is called e.
     - an integer that is called f.
     - an integer that is called g.
     - the variable that is called g = 4 is less than 5;
     - the variable that is called b = 4 is greater than 5;
     - the variable that is called c = 4 is less or equal to 5;
     - the variable that is called d = 4 is greater or equal to 5;
     - the variable that is called e = 4 is not equal to 5;
     - the variable that is called f = 4 is equal to 5;
 .