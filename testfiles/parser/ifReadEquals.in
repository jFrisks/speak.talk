A function that is called a. It returns an integer by running the following code:
    - if the function that is called the variable that is called read is equal to 9, do the following:

    .
    - if the function that is called the variable that is called read is equal to 8, do the following:
        - run the function that is called print and uses the arguments of 2;
    .
    - else, do:
        - run the function that is called print and uses the arguments of 2;
        - finally return 1;
    .
.