A function that is called f and uses the arguments of an integer that is called x. It returns an integer by running the following code:
  - finally return the negative of the variable that is called x multiplied with the negative of 2.
.