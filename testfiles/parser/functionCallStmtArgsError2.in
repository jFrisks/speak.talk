A function that is called b and returns an integer by using the arguments of an integer that is called y,. It runs the following:
    - an integer that is called x.
    - the variable that is called x has the value of the function that is called a and uses the arguments of 1,2,3.
.