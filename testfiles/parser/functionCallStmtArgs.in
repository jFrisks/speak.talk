A function that is called a and uses the arguments of an integer that is called x, an integer that is called y, an integer that is called z. It returns an integer by running the following code:
 .

A function that is called b and uses the arguments of an integer that is called y. It returns an integer by running the following code:
   - an integer that is called x.
   - the variable that is called x has the value of the value of the function a that uses the arguments of 1,2,3.
.