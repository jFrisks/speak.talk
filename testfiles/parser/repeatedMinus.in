A function that is called f and returns an integer. It runs the following:
  - finally return the negative of the negative of 3. // = the negative of (the negative of  the expression, 3)
.

A function that is called g and returns an integer. It runs the following:
  - finally return the negative of the negative of the negative of 3.
.

A function that is called g and returns an integer. It runs the following:
  - finally return the negative of the negative of the negative of the negative of the negative of the negative of 3.
.