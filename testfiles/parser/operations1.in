A function that is called a and returns an integer. It runs the following:
 - an integer that is called x.
 - an integer that is called y.
 - the variable that is called y has the value of 4.
 - the variable that is called x has the value of 4 added to 5 multiplied with the variable that is called y.
.