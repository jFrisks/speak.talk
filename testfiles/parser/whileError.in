A function that is called a and returns an integer. It runs the following:
 - an integer that is called x.
 - an integer that is called y.
 - the variable that is called x has the value of 10.
 - the variable that is called y has the value of 11.
 - loop while . Run the following  each time:
  - run the function that is called print and uses the arguments of the variable that is called x.
  - run the function that is called print and uses the arguments of the variable that is called y.
 .
.