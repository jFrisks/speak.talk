A function that is called a. It returns an integer by running the following code:
  - an integer that is called x.
  - the variable that is called x = the value of the function b that uses the arguments of the variable that is called ,1;
  - run the function that is called b and uses the arguments of the variable that is called ,1;
.

A function that is called b. It returns an integer by running the following code:

.